# 📝 Notes App (Flask + Docker + AWS)

A simple Notes App built with Flask, containerized using Docker, and ready to deploy on AWS EC2.

## 🚀 Run Locally
```bash
git clone https://github.com/harsh110306/notes-app.git
cd notes-app
docker-compose up --build
